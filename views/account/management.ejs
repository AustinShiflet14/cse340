<% if (title) { %>
<h1><%= title %></h1>
<% } %>

<section class="account-management">
    <%- messages() %>
    <% if (errors) { %>
        <ul class="notice">
    <% errors.array().forEach(error => { %>
        <li><%= error.msg %></li>
    <%  }) %>
        </ul>
    <% } %>

    <p>You're logged in.</p>
</section>